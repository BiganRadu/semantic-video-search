.venv/
data/
scratch/
# Model weights staged for upload to Kaggle. Gigabytes, and reproducible
# from Hugging Face with scripts/fetch_models.py.
models/*
!models/README.md
!models/*/dataset-metadata.json
bin/
*.pyc
__pycache__/
.env
web/node_modules/
web/dist/

# Kept out of the repo: the test suite and the corpus loader. Development
# scaffolding, not part of what the project ships. The QVHighlights eval
# harness that used to be listed here is gone -- the corpus it measured was
# deleted, so the code went with it.
tests/
eval/
scripts/
